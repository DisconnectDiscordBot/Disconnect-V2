// Variables
const { client } = require('../bot');

// When the client is logged in
client.on('ready', async () => {
	// Load the databse
	// Load the commands
	// Load the events
	// Setup extra client values
});
